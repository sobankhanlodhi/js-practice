// Manipulate or append to the array end with push()

var ourArray = [["sk", 24], ["kk", 20] ];

//appending ["bilo", 12] to ourArray
ourArray.push(["bilo", 12]);

//accessing the last element of the array
console.log(ourArray[ourArray.length - 1]);
