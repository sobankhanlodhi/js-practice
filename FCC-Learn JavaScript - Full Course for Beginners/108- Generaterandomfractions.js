// generate random decimals/fractions

//math.random() will generate random decimal numbers b/w 0 and 1 
// random number can be 0 but never be 1

function randomFraction(){
    return Math.random();
}

console.log(randomFraction());