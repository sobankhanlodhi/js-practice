// Access Array Data with indexes

//we can use the same bracket notation [ ] to access the array elements

var ourArray = ["sk", "kk", 24, 23, true];
var firstElem = ourArray[0];        //sk

console.log(firstElem);