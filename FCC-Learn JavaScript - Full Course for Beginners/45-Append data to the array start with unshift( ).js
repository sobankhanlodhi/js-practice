// Manipulate or append to the array start with unshift()

//unshift() works same like push() but with one difference which is 
//it add array element to the start
var ourArray = [["sk", 24], ["kk", 20] ];

//appending ["bilo", 12] to ourArray start
ourArray.unshift(["bilo", 12]);

console.log(ourArray);
