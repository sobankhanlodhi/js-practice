// Manipulate or Remove data from the array start with shift( )

//shift() works same like pop but with one difference which is 
//it remove array element from the start

var ourArray = ["Soban", ["kk", 20], "Bye!!!" ];

var remElementFromStart = ourArray.shift();

console.log(remElementFromStart, ourArray);
